        // const-konstante mit string-wert
        const rot = "rot";
        console.log(rot);

        /*
        let-Blockvariable mit Boolean-wert
        Beispiel { let x = 0}; }
        */
        let isHot = false;
        isHot = true;
        console.log("isHot:" ,isHot);

        // let-variable für Dezimalzahl
        let total = 1345.50;
        total =total + 1000;
        console.log(total);

        // let-variable für Ganzzahl
        let anzahl = 15;
        anzahl = anzahl + 20;
        console.log(anzahl);

        // let-variable mit string in Alert-Fenster anzeigen
        let nachricht = "Fertig - stimmt das?";
        alert(nachricht);